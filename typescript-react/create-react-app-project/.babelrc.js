// tsxを有効にする
// const presetTypeScript = ['@babel/preset-react', { isTsx: true }]

module.exports = {
  presets: [
    '@babel/preset-env',
    '@babel/preset-react',
    '@babel/preset-typescript'
  ]
}
